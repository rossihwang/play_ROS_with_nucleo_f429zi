syntax = "proto3";

message Encoder {
    enum Direction {
        FORWARD = 0;
        BACKWARD = 1;
    }
    uint32 lcounter = 1;
    Direction ldir = 2;
    uint32 rcounter = 3;
    Direction rdir = 4;
}